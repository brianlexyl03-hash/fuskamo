# Monitoring

Nothing wired up yet — Supabase's dashboard covers basic usage/query stats for now. Add Firebase Crashlytics or Sentry here once there's real user traffic to justify it (most tools have limited free tiers).
