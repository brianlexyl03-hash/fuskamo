import 'package:flutter/material.dart';

/// Design tokens — carried over 1:1 from the original pitch-themed design
/// (green turf, black night, amber floodlights). Nothing decorative that
/// doesn't belong on a pitch.
class AppColors {
  AppColors._();

  static const Color green = Color(0xFF00E664);
  static const Color amber = Color(0xFFFFB800);
  static const Color red = Color(0xFFFF3D3D);
  static const Color black = Color(0xFF050505);
  static const Color surface = Color(0xFF0F0F0F);
  static const Color card = Color(0xFF131313);
  static const Color border = Color(0xFF1F1F1F);
  static const Color muted = Color(0xFF444444);
  static const Color sub = Color(0xFF777777);
  static const Color text = Color(0xFFF0F0F0);
}
