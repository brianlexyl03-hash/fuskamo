# Design

`css/style.css` from the earlier web prototype encodes the agreed design tokens (colors, spacing) — ported directly into `lib/theme/app_theme.dart` and `lib/constants/app_colors.dart`. No separate Figma/image mockups exist yet; add them to the subfolders here if formal design docs are needed.
