No scheduled jobs yet. If you need one (e.g. nightly digest of pending
submissions), add it here and register it in `cron/` using `node-cron` —
kept separate from `notifications/` and `email/` so a job's *schedule*
and its *action* stay decoupled.
