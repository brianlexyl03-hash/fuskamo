Distinct from `uploads/` — this is for any server-generated files (e.g. a
PDF scouting report) the backend itself creates and needs to persist.
Nothing generates files yet, so this stays empty on purpose.
