# FUSKAMO Web App

This is the user-facing web client, separate from `admin-web`.

It mirrors core FUSKAMO product areas: identity/auth, home/posts, discovery, stories, reels shell, groups, messaging shell, scoreboard, universal search and profile/safety.

Configure `config.js` from `config.example.js`. Never place server-only secrets in browser code.
