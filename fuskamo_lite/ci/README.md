# CI

No pipeline yet. Once `test/` has real coverage beyond the smoke test, add a GitHub Actions workflow here running `flutter test` and `flutter analyze` on every PR. A build-and-sign workflow for the `.aab` can follow once a release keystore exists (see `release/README.md`).
