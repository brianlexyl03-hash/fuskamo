class VideoLaunchConfig {
  static const bool cloudStorageEnabled = false;
  static const String title = 'VIDEO FEATURES — COMING SOON';
  static const String message = 'We have built the video system, but cloud storage and processing are not enabled yet. Support FUSKAMO 💖 so we can buy the storage and bring full video hosting online.';
  static const int maxUploadBytes = 50 * 1024 * 1024;
  static const int maxDurationMs = 180000;
}
